name: hospital_server
description: >-
  Mini-Hospital 2026 - the REST API that fronts each application's PostgreSQL
  database. One binary, launched once per application.
publish_to: "none"
version: 1.0.0

environment:
  sdk: ^3.9.0

dependencies:
  args: ^2.5.0
  postgres: ^3.4.0
  shelf: ^1.4.0
  shelf_router: ^1.1.4

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
